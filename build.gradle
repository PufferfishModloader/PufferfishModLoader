buildscript {
    repositories {
        mavenCentral()
        maven {
            url 'https://jitpack.io'
        }
    }
    dependencies {
        classpath 'com.github.PufferfishModLoader:PufferfishGradle:c56a15d' // 🦀
    }
}

plugins {
    id 'java'
}

apply plugin: 'dev.cbyrne.pufferfishmodloader.gradle'

group 'dev.cbyrne'
version '1.0.0'

minecraft {
    separateVersionJars true
    target '1.15.2'
    mods {
        test {

        }
    }
}

repositories {
    mavenCentral()
    maven {
        url 'https://jitpack.io'
    }
}

dependencies {
    implementation group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
    implementation group: 'org.apache.logging.log4j', name: 'log4j-core', version: '2.13.2'
}
